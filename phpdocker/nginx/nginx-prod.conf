server {
    charset utf-8;
    client_max_body_size 128M;

    listen 80;
	server_name api.umjetnostudrvetu.ba;

	root /application/api/web;
    index index.php;

    access_log  /application/api-nginx-access.log;
    error_log   /application/api-nginx-error.log;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    if (!-e $request_filename) {
        rewrite ^.*$ /index.php last;
    }

	location / {
        # Redirect everything that isn't a real file to index.php
        try_files $uri $uri/ /index.php$is_args$args;
    }

	# deny accessing php files for the /assets directory
    location ~ ^/assets/.*\.php$ {
        deny all;
    }

    location ~ \.php$ {
        fastcgi_pass php-fpm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PHP_VALUE "error_log=/var/log/nginx/application_php_errors.log";
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
        include fastcgi_params;
    }
}


server {
    charset utf-8;
    client_max_body_size 128M;

    listen 443;
	server_name api.umjetnostudrvetu.ba;


    ssl    on;

    ssl_certificate /application/data/ssl/certs/cert.pem;
    ssl_certificate_key /application/data/ssl/private/key.pem;

	root /application/api/web;
    index index.php;

    access_log  /application/api-nginx-access.log;
    error_log   /application/api-nginx-error.log;


    if (!-e $request_filename) {
        rewrite ^.*$ /index.php last;
    }

	location / {
        # Redirect everything that isn't a real file to index.php
        try_files $uri $uri/ /index.php$is_args$args;
    }

	# deny accessing php files for the /assets directory
    location ~ ^/assets/.*\.php$ {
        deny all;
    }

    location ~ \.php$ {
        fastcgi_pass php-fpm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PHP_VALUE "error_log=/var/log/nginx/application_php_errors.log";
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
        include fastcgi_params;
    }
}

server {
    charset utf-8;
    client_max_body_size 128M;

    listen 80;

    server_name www.umjetnostudrvetu.ba umjetnostudrvetu.ba;

    return 302 https://$server_name$request_uri;
}

server {
    server_name umjetnostudrvetu.ba www.umjetnostudrvetu.ba;

    access_log            /var/log/nginx/ssr_designresilience.com.access.log;
    error_log             /var/log/nginx/ssr_designresilience.com.error.log;

    location / {
        proxy_pass http://139.59.149.76:4004;
        proxy_set_header Host            $host;
        proxy_set_header X-Forwarded-For $remote_addr;
    }

    sendfile off;

    listen 443 ssl;
    ssl_certificate /application/data/ssl/certs/cert.pem;
    ssl_certificate_key /application/data/ssl/private/key.pem;

}

server {
    charset utf-8;
    client_max_body_size 128M;

    listen 80;

    server_name www.admin.umjetnostudrvetu.ba admin.umjetnostudrvetu.ba;

    return 302 https://$server_name$request_uri;
}

server {
    server_name www.admin.umjetnostudrvetu.ba admin.umjetnostudrvetu.ba;
    charset utf-8;
    client_max_body_size 128M;
    listen 443;
    ssl    on;
    ssl_certificate /application/data/ssl/certs/cert.pem;
    ssl_certificate_key /application/data/ssl/private/key.pem;

    root        /application/backend/web/;
    index       index.php;

    access_log  /application/backend-nginx-access.log;
    error_log   /application/backend-nginx-error.log;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        # Redirect everything that isn't a real file to index.php
        try_files $uri $uri/ /index.php$is_args$args;
    }

    location ~ \.php$ {
        fastcgi_pass php-fpm:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PHP_VALUE "error_log=/var/log/nginx/application_php_errors.log";
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
        include fastcgi_params;
    }
}
